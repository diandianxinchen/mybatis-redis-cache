spring:
  datasource:
    url: jdbc:mysql://localhost:3306/test
    username: root
    password: 123456
    driver-class-name: com.mysql.jdbc.Driver
    type: com.alibaba.druid.pool.DruidDataSource
  redis:
    host: localhost
    port: 6379
mybatis:
  configuration:
    cache-enabled: true
  mapper-locations: classpath:mapper/*.xml
logging:
  level:
    com.example.mybatisrediscache.dao: debug